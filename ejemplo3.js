let ocupacionCanchas = true 
let nombreDeCancha = "bernabeu"
let mensaje = ocupacionCanchas ? `la cancha  llamada ${nombreDeCancha} esta ocupada"`: 
    `la cancha llamada " ${nombreDeCancha} "esta disponible"`;
console.log(mensaje);